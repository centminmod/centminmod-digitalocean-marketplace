{
  "variables": {
    "do_token": "{{env `TOKEN`}}",
    "do_image_name": "centos7-packer-snapshot-php71-{{timestamp}}",
    "do_image": "centos-7-x64",
    "do_region": "nyc3",
    "do_size": "c-2",
    "do_tags": "cmm"
  },
  "builders": [
    {
      "type": "digitalocean",
      "api_token": "{{user `do_token`}}",
      "image": "{{user `do_image`}}",
      "region": "{{user `do_region`}}",
      "size": "{{user `do_size`}}",
      "ssh_username": "root",
      "snapshot_name": "{{user `do_image_name`}}",
      "tags": "{{user `do_tags`}}"
    }
  ],
  "provisioners": [
    {
      "type": "shell",
      "scripts": [
        "scripts/cmm-install71.sh"
      ],
      "remote_folder": "/home",
      "remote_file": "script_1.sh"
    }
  ]
}